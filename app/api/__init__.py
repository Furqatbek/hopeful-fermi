"""HTTP and WebSocket routers. Thin: DTO mapping and module service calls only.

Not yet implemented — build phase 4+ (see ADR-0001 section 12). The package exists
now so the import-linter layer contract is enforceable from the first commit that
adds a router, rather than being switched on later when it is already violated.
"""
