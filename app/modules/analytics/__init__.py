"""Read models: cohort progression, item analysis, attendance.

Not yet implemented — build phase 6. This module reads NOTHING from other modules
at runtime; it is fed by domain events. The import-linter contract enforcing that
is already active, which is why the package exists before its contents do.
"""
